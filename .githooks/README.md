# Git hooks

Hooks in this folder clean commit messages (e.g. remove IDE branding).

**Enable for this repo (run once):**

```bash
git config core.hooksPath .githooks
```

After that, all commits will use these hooks.
