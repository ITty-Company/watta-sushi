/**
 * Hero на сторінці доставки — той самий порядок роликів, що був на головній раніше
 * (спочатку hero-untitled-design). Головний hero головної (`WATTA_HOME_HERO_VIDEO_SOURCES`) не змінюється.
 */
export const DELIVERY_HERO_VIDEO_SOURCES = [
  '/hero-untitled-design.mp4',
  '/watta-sushi-2-hero.mp4',
  '/welcome.mp4',
] as const
